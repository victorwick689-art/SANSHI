const API_BASE = import.meta.env.VITE_API_BASE || 'http://localhost:5000'
export async function stkPush({ phone, amount, accountReference, description }){ const res = await fetch(`${API_BASE}/mpesa/stkpush`,{ method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ phone, amount, accountReference, description })}); return res.json() }